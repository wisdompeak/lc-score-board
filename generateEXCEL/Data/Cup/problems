对于两个字符串x和y，我们定义f(x,y)表示字符串y完整连续地在字符串x中出现的次数。 比如说f(abcabb,ab)=2.
现在给定三个字符串u,s,t.字符串s和t都是完全给定的。但是u的部分字符给定了，同时另外部分字符待定(用*表示)，比如u=abc***de.
要求你能找到一个字符串u（准确地说是确定u中*的部分），使得result = f(u,s)-f(u,t)的值最大。答案只要输出result就行。

代码函数头的定义（C++）：
int solve(string u, string s, string t)

范围：
1.u的长度在1到1000之间，仅包含小写字母或者星号。
2.s和t的长度在1到50之间，仅包含小写字母。s和t不会相同。

要求：
你的代码时间复杂度必须尽可能地优秀。群主有权自行判断TLE。

